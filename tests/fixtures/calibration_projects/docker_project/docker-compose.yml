version: "3.8"

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DB_PASSWORD=example

  db:
    image: postgres:15
    environment:
      - POSTGRES_PASSWORD=secret
